<Window x:Class="TasksDK.View.ResultsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TasksDK.View"
        mc:Ignorable="d"
        Title="ResultsWindow" Height="250" Width="400">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="20"/>
            <RowDefinition Height="4*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Комментарий" VerticalAlignment="Bottom" Margin="10,0,0,0"/>
        <TextBox Name="CommentBox" Grid.Row="1" Margin="10,0,10,10" TextWrapping="Wrap" AcceptsReturn="True" AcceptsTab="True"/>
        <StackPanel Grid.Row="2" Margin="10,0,10,0" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Width="80" Height="30" Content="OK" IsDefault="True" Margin="5,0,5,0" Click="OK_Button_Click"/>
            <Button Width="80" Height="30" Content="Cancel" IsCancel="True" Margin="5,0,5,0"/>
            
        </StackPanel>
    </Grid>
</Window>
